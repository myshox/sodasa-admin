# 蘇打石器 GM 網頁版 使用說明

## 專案結構

```
SODAGMTOOL/
├── Project/          ← 原本 WinForms .exe（不影響）
├── WebApi/           ← 後端 API（ASP.NET Core，Port 5050）
└── WebApp/           ← 前端 PWA（React + Vite）
```

---

## 第一步：設定資料庫連線

編輯 `WebApi/appsettings.json`：

```json
{
  "ConnectionStrings": {
    "Default": "Server=你的IP;Port=3306;Database=soda;User=root;Password=你的密碼;"
  },
  "GmAccounts": [
    { "Username": "admin", "Password": "改成你的密碼", "Role": "superadmin" }
  ]
}
```

---

## 第二步：啟動後端 API

```
雙擊 WebApi/start-api.bat
```

或在 WebApi 資料夾執行：
```
dotnet run
```

API 會在 http://localhost:5050 啟動。

---

## 第三步A：開發模式（本機測試）

```
雙擊 WebApp/start-web.bat
```

瀏覽器開啟 http://localhost:5173

---

## 第三步B：正式部署（給其他設備使用）

### 方式一：IIS / Nginx（推薦）

1. 在 WebApp 執行 `npm run build`，產生 `dist/` 資料夾
2. 將 `dist/` 內容部署到 Web Server（IIS 或 Nginx）
3. 設定所有請求 fallback 到 `index.html`（SPA 路由用）
4. 設定 API 反向代理：`/api/*` → `http://localhost:5050`

### 方式二：快速本機分享（同一區域網路）

執行 `npm run preview`，Vite 會在 `http://0.0.0.0:4173` 提供服務，
同網路其他設備可以透過你的 IP 訪問，例如 `http://192.168.1.100:4173`

---

## PWA 安裝（手機 / 平板）

1. 在 Chrome/Edge 開啟網頁
2. 點擊網址列右側「安裝」圖示 或 「加到主畫面」
3. 即可像 App 一樣使用（無框全螢幕）

---

## 功能列表

| 頁面 | 功能 |
|------|------|
| Dashboard | 玩家總數、在線數、封號數、今日新增、全服金幣/水晶 |
| 玩家管理 | 搜尋玩家、查看詳情、加金幣/水晶、封號/解封 |
| 線上玩家 | 即時查看在線玩家清單 |
| GM 寵物指令 | 產生 petmake / petmakeabi 指令並一鍵複製 |

---

## 注意事項

- **資料庫連線**：API 和 MySQL 需要在同一台機器或網路可達
- **安全性**：正式上線前請修改 `appsettings.json` 中的 JWT Secret 和 GM 帳號密碼
- **原有 .exe**：完全不受影響，繼續正常使用
